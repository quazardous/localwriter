name: writer
description: Writer document tools (outline, content, comments, styles, tables, tracking, images)
requires: [document, config, format, image]

config:
  max_content_chars:
    type: int
    default: 50000
    min: 1000
    max: 500000
    widget: number
    label: Max Content Size
    public: true
