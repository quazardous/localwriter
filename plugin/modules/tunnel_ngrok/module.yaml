name: tunnel.ngrok
description: Ngrok tunnel provider
requires: [tunnel_manager]

config:
  authtoken:
    type: string
    default: ""
    widget: password
    label: Auth Token
    helper: "Your ngrok authentication token"
